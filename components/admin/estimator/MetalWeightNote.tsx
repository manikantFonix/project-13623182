'use client';

export default function MetalWeightNote() {
  return (
    <p className="text-[12px] leading-relaxed text-[var(--muted-text)]">
      The derivation that gives the metal weight figure its shape is not yet defined.
    </p>
  );
}